# 🖱️ FindMyMouse (macOS-style for Windows)

Простая и эффективная системная утилита на **C (Win32 API)**, которая помогает мгновенно найти курсор на большом мониторе, увеличивая его при "встряхивании". Аналог знаменитой функции из macOS.

## ✨ Особенности
*   **Shake to Find:** Увеличение срабатывает только при резких движениях в разные стороны (зигзагах).
*   **Умная фильтрация:** Алгоритм игнорирует быстрые линейные перемещения мыши через весь экран.
*   **System Tray:** Работает в фоне, не занимает место в панели задач, управляется через иконку в трее.
*   **Watchdog System:** Дополнительный процесс-страж (`MouseGuard`) гарантирует восстановление системного курсора, даже если основное приложение будет убито через Диспетчер задач.
*   **Native & Lightweight:** Написано на чистом C, потребляет около 2-4 МБ оперативной памяти.

## 🛠️ Сборка и установка

Проект разработан в среде **CLion** (JetBrains) с использованием **CMake**.

1.  **Подготовка ресурсов:**
    *   Положите файл иконки `icon.ico` в корень проекта.
    *   Убедитесь, что файл `resource.rc` содержит строку: `101 ICON "icon.ico"`.
2.  **Компиляция:**
    *   Откройте проект в CLion.
    *   Соберите таргет `Guard` (скомпилируется в `MouseGuard.exe`).
    *   Соберите основной таргет `FindMyMouse`.
3.  **Запуск:**
    *   Запустите `FindMyMouse.exe`. Приложение автоматически подтянет стража из той же папки.

## ⚙️ Настройка (main.c)

Вы можете изменить поведение утилиты, отредактировав макросы в начале `main.c`:


| Параметр | Описание | Значение по умолчанию |
| :--- | :--- | :--- |
| `MAX_SCALE` | Во сколько раз увеличится курсор | `4.0f` |
| `GROW_SPEED` | Скорость "раздувания" | `0.6f` |
| `DECAY_SPEED` | Скорость затухания эффекта | `0.3f` |
| `THRESHOLD` | Порог чувствительности встряхивания | `40` |

## 🛡️ Безопасность

Приложение временно заменяет системный указатель на прозрачный во время анимации. 
*   Если программа завершится штатно или через трей — курсор восстановится сам.
*   Если процесс будет "убит" — `MouseGuard.exe` восстановит курсор в течение 1 секунды.
*   В крайнем случае: нажмите `Win+R`, введите `main.cpl`, перейдите во вкладку "Указатели" и нажмите "По умолчанию".

## 📜 Лицензия
MIT License. Используйте и модифицируйте как угодно!
